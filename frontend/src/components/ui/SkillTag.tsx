export const SkillTag = ({ skill }: { skill: string }) => (
  <span className="inline-flex items-center px-2.5 py-1 rounded-md text-xs font-medium bg-primary/10 text-primary border border-primary/10">
    {skill}
  </span>
);
